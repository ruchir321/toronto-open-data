# Toronto Public Library Analysis

Analysis of Toronto Public Library usage, including circulation, visits, and branch information.

## Code Used
- `streamlit_dashboard.py`

## Data Used
- No data files found locally (data may be fetched dynamically).

## How to Run
1. **Initialize Environment**:
   ```bash
   uv init
   uv sync
   ```
2. **Run Code**:
   Navigate to the `code/` directory and run the scripts or notebooks.
   ```bash
   cd code
   # Example:
   python streamlit_dashboard.py
   ```
